# GraphQL schema example
#
# https://gqlgen.com/getting-started/

input NewUser {
  email: String!
  first_name: String!
  last_name: String!
}

type Query {
  getUser(id: ID!): User!
}

type Mutation {
  createUser(input: NewUser!): User!
}

schema {
  query: Query
  mutation: Mutation
}
