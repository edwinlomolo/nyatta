# Represent property unit tenant
type Tenant {
  id: UUID!
  startDate: Time!
  endDate: Time
  propertyUnitId: UUID! # belongs-to property unit
  userId: UUID!
  user: User!
  propertyUnit: PropertyUnit # vacancy dictates nullity
  createdAt: Time
  updatedAt: Time
}
