# Represent property unit tenant
type Tenant {
  id: ID!
  startDate: Time!
  endDate: Time
  upload: [AnyUpload!]!
  propertyUnitId: ID! # belongs-to property unit
  createdAt: Time
  updatedAt: Time
}
